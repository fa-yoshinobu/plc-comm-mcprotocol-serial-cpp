# RJ71C24-R2 RS-232C Report

Audience: maintainers and users who need the current validated view of the `RJ71C24-R2` path.

This page is the single report for this hardware target. Older dated `Format4` and `Format5` logs
were consolidated here so the current state does not fragment across multiple files.

## Target

- PLC CPU: Mitsubishi iQ-R `R08CPU`
- Serial module: `RJ71C24-R2`
- Link: `RS-232C`

## Current Validated Setups

### Format4 ASCII

- Settings: `19200 / 8E1 / MC Protocol Format4 ASCII / CRLF / sum-check off / station 0`
- Validated scope:
  - `cpu-model`
  - `loopback`
  - contiguous `0401/1401` read/write
  - host buffer `0613/1613`
  - module buffer `0601/1601`
  - qualified helper `read-qualified-words` / `write-qualified-words`
- Operational note:
  - after timeout or mixed-response states, ASCII `EOT CRLF` or `CL CRLF` recovers the C24
    transmission sequence

### Format5 Binary

- Settings: `28800 / 8E2 / MC Protocol Format5 Binary / sum-check on / station 0`
- Practical family selection: `ql`
- Validated scope:
  - contiguous `0401/1401` read/write
  - native `0403`
  - native `1402` words
  - native `1402` bits
  - native `0406`
  - native `1406`
  - native `0801/0802`
- Additional iQ-R-only spot results:
  - `2026-04-12` on `R120PCPU + --series iqr`, `read-bits SM0 4`, `read-words SD0 2`, and
    `read-words RD0 2` passed
  - `2026-04-12` on `R120PCPU + --series iqr`, `read-words LTN0 4`, `read-words LSTN0 4`, and
    `read-words LCN0 2` passed
  - `2026-04-12` on `R120PCPU + --series iqr`, `random-read LTN0`, `LSTN0`, and `LCN0` returned
    `0x7F23`
  - `LZ` remained hold under `--series iqr`

## Summary

| Area | Current status | Notes |
|---|---|---|
| Contiguous read/write | pass | validated on both Format4 ASCII and Format5 Binary |
| Host/module buffer | pass | validated on the practical paths |
| Qualified helper access | pass, narrow scope | helper `U3E0\\HG20` read/write/restore passed; helper `U3E0\\G10` spot read returned `0x83BD` |
| Native random/write/multi-block/monitor | pass on Format5 Binary | use `--series ql` |
| iQ-R-only spot reads | partial pass on Format5 Binary | use `--series iqr`; `SM0`, `SD0`, and `RD0` passed on `R120PCPU`, while `LZ0/LZ1` random-read still returned `0x7F23` |
| Long current-value spot reads | partial pass on Format5 Binary | use `--series iqr`; `read-words LTN0 4`, `read-words LSTN0 4`, and `read-words LCN0 2` passed on `R120PCPU`, while `random-read LTN0`, `LSTN0`, and `LCN0` returned `0x7F23` |
| `LZ` double-word random-read | hold | local support now treats `LZ` as an iQ-R-only double-word device; `--series ql` is rejected locally and `--series iqr` `random-read LZ0`, `LZ1`, and `LZ0 LZ1` returned `0x7F23` on `2026-04-11` |
| Native qualified access | unsupported / diagnostic only | helper and native semantics still disagree; helper-only is the supported path |

## Series Selection Note

On `RJ71C24-R2 + R08CPU`, the practical native subcommand family for random, write, multi-block,
and monitor traffic is `--series ql`.

Observed follow-up behavior:

- `--series ql`: native `0403`, `1402`, `0406`, `1406`, `0801`, and raw `0802` pass
- `--series iqr`: false negatives remain on random and monitor traffic

Keep `--series iqr` only as a probe mode on this target.

For iQ-R-only device surfaces, use `--series iqr`:

- `2026-04-12` spot reads on `R120PCPU` showed `SM0`, `SD0`, and `RD0` passing under
  `Format5 Binary`
- `2026-04-12` spot batch reads on `R120PCPU` showed `LTN0`, `LSTN0`, and `LCN0` passing under
  `Format5 Binary` when requested as `read-words LTN0 4`, `read-words LSTN0 4`, and
  `read-words LCN0 2`
- `2026-04-12` spot random-read probes on `R120PCPU` showed `LTN0`, `LSTN0`, and `LCN0`
  returning `0x7F23`
- `LZ` remained hold under the same conditions

## Qualified Access Note

Qualified helper access remains the practical public path on this target:

- helper `read-qualified-words 'U3E0\\G10' 1` returned `0x83BD`
- helper `read-qualified-words 'U3E0\\HG20' 1` returned `0x0000`
- helper `write-qualified-words 'U3E0\\HG20' ...` readback and restore passed

Native qualified access is not part of the supported workflow on this target:

- historical probes produced `0x7F22`, timeout, `0x4031`, and semantically mismatched success
- no native qualified read/write path should be treated as validated or supported

## Historical Notes

- Earlier dated `Format4` and initial `Format5` reports contained native failures that were later
  superseded by encoder fixes and corrected `--series` selection.
- The canonical matrix is [HARDWARE_VALIDATION.md](HARDWARE_VALIDATION.md).
